This module can be imported as an eclipse project, it contains:

A remote control that associates a GUI with Sifau commands. This allows users
to operate siafu remotely, without having to interface with the GUI on the 
computer that runs the simulation. Specially useful for demos! 
